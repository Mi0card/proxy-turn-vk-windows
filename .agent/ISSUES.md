# Issues — the tracked backlog: bugs, debt, deferred work. One line each, newest first.
# If "we should fix that later" isn't a line here, it will be forgotten. Journal `flag:` lines are
# the informal inbox; maintain.md promotes the ones that matter into this file and drops the rest.
# Scan Open before designing or when picking what to work on; FIXING an issue is routed like any
# other request (patch/debug/feature) — this file only tracks, it never carries the work itself.
# Format: `I<n> <YYYY-MM-DD> P<1|2|3> [scope] symptom or task — evidence/repro (refs: D<n>, design, S<n>)`
#   P1 broken for users now · P2 wrong or risky, schedule it · P3 debt/idea, fix when passing.
#   Next <n> = highest number anywhere in this file + 1. One line; at most one indented detail
#   line. Needs more? Stub a design in .agent/designs/ and reference it — don't bloat this file.
# Close = move the line under Closed and append ` → closed <YYYY-MM-DD> S<n>: <fix or wontfix + why>`.
# Caps: Open ≤40 (over → merge duplicates, close the stale, demote to P3 or drop) ·
# Closed ≤100 (maintain.md deletes the oldest lines; git history keeps everything forever).

## Open (newest first — scan this section only)
- (none — backlog empty; next id I22)

## Closed (append-only history; grep it, never load it wholesale)
- I21 2026-09-10 P3 [tests] no tests for the recovery hotspot (readStream, checkCircuitBreaker, watchdog, restartTunnel, TunnelStart) or proxy relay/system-proxy toggle (SystemProxyEnable/Disable, handleHTTP/handleSocks5/handleConnect) — blocks refactor.md cleanup there (audit S14) → closed 2026-09-10 S15: proxy_test.go gained TestCopyHeader/TestStripHopByHopHeaders/TestRelayBidirectional + sysProxyHandler 502-without-tunnel tests; recovery internals and the registry enable/disable path remain untestable (Wails EventsEmit log.Fatal on nil ctx; real WinINET registry) — recorded in the test file, not silently dropped.
- I20 2026-09-10 P3 [harness] PROJECT.md stale: :31 says AppVersion at app.go:29 = 0.2.10.0 (actual app.go:30 = 0.3.0.2); :46 claims .agent//CLAUDE.md/AGENTS.md are git-ignored but all are tracked (audit S14) → closed 2026-09-10 S14: both lines corrected during the audit.
- I19 2026-09-10 P3 [frontend] index.html hardcodes v0.2.5.7 vs AppVersion 0.3.0.2 (index.html:63 vs app.go:30); listen/workers/proxy/deploy defaults also duplicated HTML↔JS and can drift (audit S14) → closed 2026-09-10 S15: version placeholder `v…` filled by GetVersion at load; JS defaults centralized in DEFAULTS captured from the HTML inputs (collectConfig/collectProfile/connect/proxyStart/deploy/undeploy).
- I18 2026-09-10 P3 [frontend] latent XSS: ts/lv interpolated into innerHTML unescaped while msg is escaped (main.js:910,1661,1808,1818); not exploitable today but any dynamic level/ts becomes XSS (audit S14) → closed 2026-09-10 S15: appendLogEntryTo escapes ts via escHtml and lv via escAttr.
- I17 2026-09-10 P3 [frontend] four near-identical log-line renderers build the same ts/lv/escHtml(msg) template (main.js:907-916,1658-1668,1803-1812,1814-1822) — every fix must be applied four times (audit S14) → closed 2026-09-10 S15: single shared appendLogEntryTo replaces all four renderers.
- I16 2026-09-10 P3 [proxy] relay logic duplicated between ProxyServer.handleHTTP and sysProxyHandler: identical relay, hop-by-hop list, header-copy loop, idleConn setup ×4 (proxy.go:660-766 vs 818-869) (audit S14) → closed 2026-09-10 S15: shared newTunnelTransport/stripHopByHopHeaders/copyHeader/relayBidirectional; remaining differences are intentional (sys-proxy is no-auth/local).
- I15 2026-09-10 P3 [deadcode] zero-caller members: GetClientExePath (app.go:716), socksStats field (app.go:179), wgDial (proxy.go:35), pf.varint written-not-read (ruleset_manager.go:587,612), iconSmall/procDestroyWindow (tray_windows.go:28,80), routingPolicyLabel (main.js:1022) — each greps to 1 hit (audit S14) → closed 2026-09-10 S15: all removed; parseField keeps the varint advance via `_, p2 := readVarint`.
- I14 2026-09-10 P3 [app] dead redundant branch: parallelStages map referenced only there and both if/else arms run the same a.log (app.go:1302-1313) (audit S14) → closed 2026-09-10 S15: map and duplicate branch removed.
- I13 2026-09-10 P2 [captcha] macOS captcha webview can block forever — <-resultCh with no timeout/select (captcha_webview_darwin.go:117) (audit S14) → closed 2026-09-10 S15: select on resultCh with 10m timeout + recover()→done(error) in the main-queue closure. NOT build/run verified (needs a real Mac).
- I12 2026-09-10 P2 [frontend] setBusy overwrites #status-badge with "Загрузка..." but never restores it (main.js:86-93) — header stuck until the next tunnel event (audit S14) → closed 2026-09-10 S15: setBusy(false) calls updateStatusBadge, which now renders connected/paused/off.
- I11 2026-09-10 P2 [frontend] window load handler awaits backend calls with no try/catch (main.js:98-153) — one rejection aborts all setup (audit S14) → closed 2026-09-10 S15: tabs + EventsOn registered first, then each backend call isolated in its own try/catch.
- I10 2026-09-10 P2 [proxy] sysProxyHandler leaks its http.Transport — no IdleConnTimeout, never CloseIdleConnections'd (proxy.go:800-808; system_proxy.go:113,169) (audit S14) → closed 2026-09-10 S15: handler stored on App, Close() closes idle conns on disable; IdleConnTimeout 90s + ReadHeaderTimeout 30s.
- I9 2026-09-10 P2 [system_proxy] partial sysProxyApplyStatic failure is not rolled back and the backup is deleted (system_proxy.go:137-144) (audit S14) → closed 2026-09-10 S15: error branch rolls back via sysProxyRestore(cur); backup cleared only when rollback succeeds, else kept for startup recovery.
- I8 2026-09-10 P2 [system_proxy] sysProxyRestore ignores every registry write error and returns nil (syscall_windows.go:101-122; system_proxy.go:176, app.go:289) — failed restore reports success and crash-recovery clears the backup (audit S14) → closed 2026-09-10 S15: returns first error (ErrNotExist tolerated); SystemProxyDisable and startup recovery log and keep the backup on failure.
- I7 2026-09-04 P3 [app] shutdown doesn't join finalizeTunnel goroutine (app.go:978) — TunnelStop kills proc but finalizeTunnel mutating tunnelRunning/sysProxyOn can race shutdown cleanup (audit S5) → closed 2026-09-07 S10: app.go finalizeDone channel signals finalizeTunnel completion; TunnelStop + quitApp wait for it (5s timeout) before proceeding.
- I6 2026-09-04 P3 [proxy] new http.Transport per non-CONNECT HTTP request (proxy.go:722), no pooling — inconsistent with sysProxyHandler which reuses one (proxy.go:777) (audit S5) → closed 2026-09-07 S9: proxy.go Transport pool (transportDirect + transportTunnel) replaces per-request creation; connection pooling enabled for non-CONNECT HTTP requests.
- I3 2026-09-04 P2 [proxy] zero test coverage on ProxyServer → closed 2026-09-04 S6: proxy_test.go added (route, conn-counters/setters, parseProxyAuth, wgB64ToHex, ParseDNSOverride); parseWGConf already covered in parse_test.go.
- I4 2026-09-04 P2 [frontend] XSS in renderRuleSuggest (main.js:1264-1277): rule `group` interpolated into data-value="${group}" + innerHTML with no escaping — corrupt .dat group name → HTML/attr injection (audit S5) → closed 2026-09-04 S8: added escAttr(), applied escAttr() to data-value attrs + escHtml() to innerHTML text (lines 1264, 1274, 1289).
- I5 2026-09-04 P3 [system_proxy] no tests for enable/disable/backup/restore (Windows-critical path) — blocks refactor.md cleanup there (audit S5) → closed 2026-09-04 S7: system_proxy_test.go added (backup path, save/load round-trip, load-no-file, load-corrupt, clear, overwrite, valid-JSON, SystemProxyStatus, SystemProxySupported, all-Had-false); sysProxyRead/sysProxyApplyStatic/sysProxyRestore are Windows-only registry ops, not cross-platform testable.
- I2 2026-09-01 P3 [harness] `.agent/` is git-ignored → agent state is local-only, cross-machine handoff impossible — decide whether to track harness state or accept local-only (ref: D1) → closed 2026-09-04: premise false — .agent/ removed from .gitignore by user.
- I1 2026-09-01 P3 [server_src] server tests can't run on Windows host (module is Linux-only; `ipc.UAPIOpen` undefined) — need a Linux env/CI job or a build-tag gate to run them (observed at bootstrap) → closed 2026-09-07: dropped by user.
