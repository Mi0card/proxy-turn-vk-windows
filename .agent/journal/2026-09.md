S1 2026-09-01 bootstrap: adopted repo — harness files filled (CLAUDE facts, MAP, PROJECT, DECISIONS D1-D4, ISSUES I1-I2); verified go build/test/vet (root, go_client, server_src cross-compile linux/amd64); build.ps1 unverified (no Wails CLI/GCC)
  flag: server_src tests unrunnable on Windows host (I1); .agent/ git-ignored → state local-only (I2)

S2 2026-09-01 wake-aware auto-restart (design 001, shipped both slices): new wake.go (wall-clock polling
  detector + shouldHaltRestarts + startWakeMonitor + onWake + ensureRestart single-flight/bounded); breaker
  flood/mismatch/refused → restart, wrap-timeout stays hard stop; watchdog routed through ensureRestart with
  identity-checks; restartTunnel now closes old watchdog chan; root go build/vet/test green (race tool blocked
  — no cgo/GCC). Manual sleep e2e still pending (needs real hardware).
  flag: duplicate-restart window between old watchdog's backoff sleep and a wake restart closed via sameProc check.