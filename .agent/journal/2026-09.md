# Journal — append-only log of completed sessions. Newest first.

S12 2026-09-09 feature (cont): dead-tunnel авто-рестарт сделан консервативным — жалоба пользователя «постоянно реконнектит». Единый гейт requestDeadRestart/Locked + pure deadTunnelDue: стартовый грейс 90 с, свежая активность воркеров (< restartStaleWindow) блокирует, кулдаун deadRestartCooldown 3 мин (не сбрасывается активностью воркеров — иначе была бесконечная петля, т.к. restartAttempts сбрасывался на «активных: N>0»). Детектор A теперь зовёт гейт вместо прямого ensureRestart; deadlineWorkers чистится и при живой активности. Детектор B: pingFailToRestart 3→5, при пороге — requestDeadRestart, при отказе сброс серии (без спама каждый тик). Тесты TestDeadTunnelDue (8 кейсов) + порог пинга. v0.3.0.1. Build/vet/test green.

S12 2026-09-09 feature (cont): qwdtt://config import added — backend App.ParseQwdtt/parseQwdttLink (query name/peer/hashes/workers/port/pass|password; обе формы qwdtt://config и qwdtt:config; peer без порта → :56000 как Android PeerAddress.ensurePort; дефолт имени "QR Профиль"), frontend importQwdtt в parseWdtt → SaveProfile/SetActiveProfile/applyProfile. Tests в parse_test.go. Root build/vet/test green; JS syntax check ok. COMMITTED f0bb564 (v0.3.0.0).

S12 2026-09-09 feature: fingerprint switching dropped repo-wide (frontend select + main.js, app.go arg/fields, go_client -fingerprint flag + TLS map → upstream fixed Chrome_146) — local layer go-client-local.patch regen to Windows-build fixes only (listen retry/dynamic-port + tun_fd stubs; verified forward-apply on pristine == repo tree, reverse-check ok). Dead-tunnel auto-restart added, always-on via ensureRestart: (A) checkCircuitBreaker counts distinct workers hitting "context deadline exceeded" (all workers in 60s window → restart); (B) pingLoop strict-tunnel probe wgDialStrictTimeout 4s, 3 consecutive fails → restart; reset in TunnelStart/wake. Config top-level fingerprint = SSH host-key kept (SaveConfig guard). Tests: deadline_test.go. COMMITTED 44a36aa.

S11 2026-09-08 feature: M11 part 1 — engine upstream migrated to SpaceNeuroX. Phase 2 done: go_client replaced (backup go_client.bak) + Phase 4: server_src replaced (backup server_src.bak). Phase 3+windows-build layer: .agent/local/go-client-local/ (patch 234 lines: profiles.go fingerprint+TLS map, main/creds/creds_vkcalls, listen.go+tun_fd.go tags, 2 new windows stubs). sync.yml/build.yml/build.ps1 re-pointed; DECISIONS D11-D13. Verified: go_client build+vet OK, server_src linux build+vet OK, root build+test OK, layer fresh/idempotent/fail-loud tested.
  flag: layer files + whole engine swap UNCOMMITTED (61 files) — commit before next sync/build; go_client/go.sum needs network (GOSUMDB=off) to regenerate after each sync. Old obfs tests dropped (API rewritten upstream).

S10 2026-09-07 maintain: sweep — all budgets within cap (STATE 24/40, MAP 21/120, PROJECT 48/80, DECISIONS 26/50, ISSUES 22/40, journal 5/200). No area docs, no designs, no scratch. No flags triaged. I1 remains open (Linux-only server tests). metrics: 6540 loc · 25 go files · 6 test files (1345 loc).

S9 2026-09-07 patch: I6 closed — proxy.go Transport pool (transportDirect + transportTunnel) replaces per-request creation; connection pooling enabled for non-CONNECT HTTP requests. go test ./... + go vet clean.

S8 2026-09-04 patch: I4 closed — frontend/main.js XSS fixed: added escAttr() (escapes &"<>), applied escAttr() to all data-value attributes + escHtml() to innerHTML text in renderRuleSuggest (lines 1264, 1274, 1289). go test ./... pass.

S7 2026-09-04 patch: I5 closed — system_proxy_test.go added (backup path, save/load round-trip, load-no-file, load-corrupt, clear, overwrite, valid-JSON, SystemProxyStatus, SystemProxySupported, all-Had-false). 10/10 PASS.

S13 2026-09-09 patch: ping-based dead-tunnel restart (detector B) DROPPED - pingLoop now measure-only (5s tick, 4s strict probe, tunnel:ping on success only); removed pingFailToRestart/deadTunnelOnPingFails/requestDeadRestart wrapper + TestDeadTunnelOnPingFails. Dead-tunnel restart now solely via all-worker context deadline timeout (detector A). README + proxy.go comments updated. v0.3.0.2. Root build/vet/test green, node --check ok. COMMITTED b2be86d (v0.3.0.2).

S14 2026-09-10 audit: 14 findings filed (6 P2, 8 P3). P2: sysProxyRestore swallows all errors (I8), partial apply no rollback + backup deleted (I9), sysProxyHandler transport leak (I10), frontend init unguarded (I11), status badge stuck (I12), macOS captcha can hang (I13). P3: dead parallelStages branch (I14), zero-caller members (I15), proxy relay duplication (I16), four log renderers (I17), latent ts/lv XSS (I18), version/default drift (I19), stale PROJECT.md (I20), recovery/proxy test gaps (I21). Cleared: proxy transport pool (I6), system-proxy tests (I5), renderRuleSuggest XSS (I4), ruleset map concurrency, protobuf parsers, local go_client layer. Files: .agent/ISSUES.md, PROJECT.md, STATE.md. build/vet/test green.

S15 2026-09-10 patch: fixed all filed audit findings I8–I21 (I20 was already fixed in S14). P2: sysProxyRestore returns first registry error (ErrNotExist tolerated) + callers keep backup on failure (I8); apply-error branch rolls back via sysProxyRestore(cur), backup cleared only on successful rollback (I9); sysProxyHandler.Close (CloseIdleConnections) on disable + IdleConnTimeout 90s/ReadHeaderTimeout 30s, handler stored on App (I10); frontend load handler registers tabs/EventsOn first then isolates each backend call in try/catch (I11); setBusy(false) restores status badge via updateStatusBadge (now handles connected/paused/off) (I12); darwin captcha select+10m timeout+recover→done(error) (I13, unverified on real Mac). P3: parallelStages map+branch removed (I14); dead members GetClientExePath/socksStats/wgDial/pf.varint/iconSmall/procDestroyWindow/routingPolicyLabel removed (I15); shared newTunnelTransport/stripHopByHopHeaders/copyHeader/relayBidirectional (I16); shared appendLogEntryTo (I17) escaping ts/lv (I18); DEFAULTS captured from HTML inputs + index.html version placeholder (I19). Tests: +6 in proxy_test.go (copyHeader, stripHopByHop, relay, sysProxyHandler 502×2, Close/idle). build/vet/test green; node --check ok; -race unavailable (CGO off, no GCC). Upstream worker default re-verified = 9 (go_client main.go:169, group.go:14; server workersPerHash:9). Bumped AppVersion (app.go:30) + wails.json → v0.3.0.3. Files: app.go, proxy.go, proxy_test.go, system_proxy.go, syscall_windows.go, ruleset_manager.go, tray_windows.go, captcha_webview_darwin.go, frontend/main.js, frontend/index.html, wails.json, .agent/ISSUES.md, PROJECT.md, DECISIONS.md, STATE.md.

S16 2026-09-15 feature: connection log reformatted to `→ host:port - app - [Nms, proxy|direct]` (block → `[block]`, error → `: ошибка: …`); app identified by local-port→PID→process basename (Windows GetExtendedTcpTable/OpenProcess/QueryFullProcessImageNameW; macOS `lsof -nP -iTCP -Fpcn`; unix stub; UA fallback; 3 s ≤256 TTL cache), and the WinINET system proxy (`sysProxyHandler`) now applies routing rules + logs in the same format. Version → v0.3.1.0 (app.go:30, wails.json:15); README/index.html text updated. Design 003 approved+shipped; DECISIONS D16 (sysproxy follows global default, was strict-tunnel) + D17 (log format/app source). Files: proxy.go, syscall_windows.go, syscall_darwin.go, syscall_unix.go, system_proxy.go, proxy_test.go, syscall_windows_test.go, syscall_darwin_test.go, app.go, wails.json, README.md, frontend/index.html, .agent/designs/003-conn-log-app-name.md, DECISIONS.md, STATE.md. build/vet/test green; GOOS=linux build ok; node --check ok; fresh-eyes review ran (no blockers) — its 3 minor findings folded in (tcpOwnerPID retry on ERROR_INSUFFICIENT_BUFFER; GET success logged after WriteHeader not after io.Copy; logField strips CR/LF to block logline forgery).
  flag: macOS `lookupProcessByPort` (lsof) unverified — no Mac on this box, same class as I13. STATE claimed "S15 uncommitted" but f2a497c (v0.3.0.3) is already committed — S15 END ran before its commit, not a crash; rebuilt STATE as S16.

S17 2026-09-17 patch: 3 user-reported fixes → v0.3.1.1. (1) Config import didn't refresh the profile list until restart: frontend extracted refreshProfiles() from initProfiles (no duplicate listeners) and applyImportedConfig awaits it; backend applyImportedConfigLocked (new, ImportConfig) runs migrateLegacyProfileLocked so a legacy import gets «Профиль 1» at once. (2) Import/export now show success toasts; SaveConfigDialog returns SaveConfigResult{ok,canceled,error} so cancel is not an error and write failures aren't swallowed; import cancel (empty error) is no longer reported as failure. (3) Ping in header was missing since 44a36aa switched pingLoop to strict wgDialStrictTimeout 4s (no direct fallback, skipped when netstack down); now wgDialFallbackTimeout (netstack else direct) + pingProbeTimeout 10s, and updateTunnelUI calls updateStatusBadge() (single badge writer) so status events stop wiping the ping. Tests: config_test.go (import migration, profiles kept), proxy_test.go TestWgDialFallbackNoTunnel. Root build/vet/test green; gofmt clean; node --check ok. Files: app.go, proxy.go, proxy_test.go, config_test.go, frontend/main.js, wails.json, .agent/STATE.md, DECISIONS.md.
