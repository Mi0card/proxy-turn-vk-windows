# Engine trees are upstream-mirrored; the local go_client layer (git apply) matches
# LF contexts, so force LF on every OS — Windows CI checkouts must not turn them CRLF.
go_client/** text eol=lf
server_src/** text eol=lf
.agent/local/** text eol=lf